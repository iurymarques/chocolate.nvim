local M = {}

M.options = {
  modules = {
    treesitter = true,
    diagnostic = true,
    cmp = true,
    telescope = true,
    gitsigns = true,
    neotree = true,
    neogit = true,
    native_lsp = true,
    dap = true,
    statusline = true,
  }
}

return M
